import Song from "./Models/Song.js";

class Store {
  /**
   * Provides access to application state data
   */
   state = {
    songs: [],
    mySongs: [],
    activeSongs: []
  };
}

/**
*@param {function} fn
*/



const store = new Store();
export default store;


//the only way to access an array dynamically is through bracket notation
//the only way to access an array dynamically is through bracket notation
//the only way to access an array dynamically is through bracket notation
//the only way to access an array dynamically is through bracket notation